spring:
  profiles:
    active: dev
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://127.0.0.1:9000"
    
  datasource:
    url: jdbc:mysql://localhost:3306/casa_te
    username: root
    password: asas1234
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect 
    show-sql: false
    hibernate:
      ddl-auto: create # ✅ Crea tablas + schema.sql + data.sql
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true